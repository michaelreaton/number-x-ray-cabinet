{
  "name": "number-x-ray-cabinet",
  "version": "1.0.0",
  "private": true,
  "description": "A self-contained browser lab for adversarially curious cyclotomic structure scans.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "smoke": "node tests/smoke-browser.cjs"
  }
}
