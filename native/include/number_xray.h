#ifndef NUMBER_XRAY_H
#define NUMBER_XRAY_H

/*
 * Stable public entry point for the Number X-Ray native C SDK.
 *
 * All exported functions and structs are declared in xray_workbench.h with
 * Doxygen-style comments. Include this wrapper in new tools so future public
 * headers can be added without changing consumer code.
 */
#include "xray_workbench.h"

#define NUMBER_XRAY_VERSION XRAY_VERSION

#endif
